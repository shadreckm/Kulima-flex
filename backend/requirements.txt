# Core web framework
fastapi
uvicorn[standard]
python-multipart
pydantic

# Auth / JWT
PyJWT

# Env loading
python-dotenv

# LLM client
openai

# Tavily OSINT client
tavily-python

# Data / viz dependencies used by kulima core (Streamlit app, portfolio, exports)
streamlit
plotly
reportlab
networkx
pandas
tenacity

# Testing (if you run tests inside the container)
pytest

# Document security (Phase 5): AES-256-GCM at-rest encryption for uploaded
# documents. Without this package, storage falls back to provider disk-level
# encryption metadata even when KULIMA_DOC_ENCRYPTION_KEY is configured.
cryptography>=42.0.0
